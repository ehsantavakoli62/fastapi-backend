# fastapi-backend

# 🚀 Final Project: Microblogging Service (FastAPI & PostgreSQL)

This repository contains the backend implementation for a corporate microblogging service, developed as the final project for the Skillbox "Python Advanced" course.

The service is built using **FastAPI** and uses **PostgreSQL** as the database, orchestrated with **Docker Compose**.

## 💻 1. Setup and Launch

To run the project, you only need to have **Docker** and **Docker Compose** installed on your machine.

**1. Clone the repository:**
(Assuming the project is already cloned/downloaded in the `fastapi-backend` folder)

**2. Launch the services:**
Execute the following command in the root directory of the project (where `docker-compose.yml` is located):

```bash
docker-compose up -d
